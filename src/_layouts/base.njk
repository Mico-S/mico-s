<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ site.title }}</title>
    <link rel="icon" type="image/png" href="{{ '/assets/favicon.png' | url }}">
    <link rel="stylesheet" href="{{ '/assets/css/style.css' | url }}">
</head>
<body>
    <header>
        <nav>
            <h1><a href="{{ '/' | url }}" class="site-title-link">{{ site.title }}</a></h1>
            <div class="nav-links">
                <a href="{{ '/' | url }}">Home</a>
                <a href="{{ '/about/' | url }}">About</a>
            </div>
        </nav>
    </header>
    <main>
        {{ content | safe }}
    </main>
    <footer>
        <p>&copy; {{ site.currentYear }} Micah Cook. All rights reserved.</p>
    </footer>
</body>
</html> 